const initialState = {
  tickets: [],
};

export const ticketReducer = (state = initialState, action: any) => {
  const newState = { ...state };
  switch (action.type) {
    default:
      break;
  }
  return newState;
};
